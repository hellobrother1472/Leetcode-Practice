#include <bits/stdc++.h>
using namespace std;

// Brute Force : Generate all the subarray with taking two variables min and max and then get the answer.

class Solution {
public:
    long long subArrayRanges(vector<int>& nums) {
        
    }
};

int main(){

    return 0;
}